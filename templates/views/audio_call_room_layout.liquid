<html>

{% render 'head_partial', page: page %}

<body class='overflow-hidden'>
  <div class='w-screen h-screen font-roboto bg-gray-800 box-border overflow-y-auto'>
    <div id='participants-wrapper' class='grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-y-10 gap-x-6 sm:gap-y-8 sm:gap-x-4 w-full p-4 sm:p-8'>
      
        <div class='flex flex-col items-center py-8 justify-evenly border-2 border-gray-200 rounded-md'>
          <h4 class='text-2xl text-gray-200 uppercase tracking-wider'>{{user.username}}</h4>
          <div class="image-container mt-10 w-32 h-32">
            <img class='w-32 h-32 rounded-full bg-white' src="{{user.profilePic}}" alt="{{user.username}}" title={{user.username}} />
          </div> 
        </div> 

    </div>

    <div class='controls w-full fixed bottom-4 flex justify-center'>
      <button id='leave-call' class='uppercase px-4 py-3 bg-red-200 text-white text-sm xs:text-base sm:text-lg md:text-xl rounded-md transparent-borderless-spinneles-element tracking-wider'>
        <i class="fas fa-phone-slash text-red-700 text-3xl"></i>
      </button>
    </div>

  </div> 

  <div class='column-wrapper hidden'>
      <div id='video-grid'>
        <video class='hidden mt-4 object-fill object-cover lg:max-h-80 max-w-[200px] lg:max-w-none'></video>
      </div>
  </div>   

  <input type='hidden' name='test' id='test' value="{{jwtUser}}" />
  <input type='hidden' name='port_num' id='port_num' value="{{port_number}}" />
</body>

<script src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
<script src="/script.bundle.js"></script>

</html>